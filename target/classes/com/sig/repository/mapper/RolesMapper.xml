<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sig.repository.mapper.RolesMapper">
    

    <select id="getAll" resultType="Roles">
        SELECT id
        	,nombre
         FROM "roles" 
         
    </select>
</mapper>